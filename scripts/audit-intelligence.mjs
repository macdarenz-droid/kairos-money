import fs from 'node:fs';
const research=fs.readFileSync('docs/RESEARCH.md','utf8').split('\n');
const claims=[['Small purchases add up; check the plan before paying','pain-of-paying'],['Review recurring commitments','mental-accounting'],['Plan the first days after pay; no impulsive intent claim','present-bias'],['Make room for essentials; calm triage','scarcity'],['Every if-then suggestion','implementation-intentions'],['Profile labels and optional enjoyment reflection are descriptive, not diagnosis','tightwad-spendthrift'],['User-defined earmarks are not additional balances','mental-accounting'],['A sustained pay increase; reserve a chosen share','save-more-tomorrow'],['Concrete future goal and target date','future-self'],['Buffer reservation framing','loss-framing'],['No manufactured urgency; plan boundaries only','fresh-start'],['Nudge feasibility and control checklist','east-com-b'],['No inferred money-script claim; optional scale disabled','money-scripts'],['No inferred enjoyment from categories','experiential-spend']];
const lines=claims.map(([claim,id])=>{const line=research.findIndex(x=>x==='## '+id)+1;if(!line)throw new Error('Unmapped claim '+id);return `| ${claim} | docs/RESEARCH.md line ${line}, ${id} |`;});
fs.writeFileSync('docs/CLAIM_AUDIT.md','# Behavioural claim audit\n\nVersion 1. Descriptive numerical measurements and scenario arithmetic are not causal psychological claims. Archetype weights are documented app heuristics. Research does not validate a personal diagnosis or a saving percentage.\n\n| User-facing claim or constraint | Research location |\n|---|---|\n'+lines.join('\n')+'\n');
console.log(`${claims.length} claim mappings verified.`);
