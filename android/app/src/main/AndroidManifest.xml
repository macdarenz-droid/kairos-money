<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The owner asked for live exchange rates and decided this app is no longer strictly offline:
         "live rate every real time / core rule, dont consider the project fully offline / there are
         features i want online."

         What that buys and what it costs. INTERNET was previously stripped with tools:node="remove", so
         "nothing leaves this device" was a property of the build rather than a claim anyone had to be
         trusted on. Granting it gives that up, and the guarantee is narrowed rather than abandoned:
         src/core/net/rates.ts is the only module permitted to reach the network, tests/no-network.test.ts
         fails the build if a second one tries, and that module cannot read the database at all — it takes
         currency codes and returns rates.

         So what leaves the phone is a currency code and a date. No amount, balance, merchant, account or
         transaction ever does. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- So the owner can pick their banking app from a list. Android 11 hides other installed apps
         unless visibility is declared; this asks only for apps with a launcher icon, which is exactly
         what the picker shows, rather than the blanket QUERY_ALL_PACKAGES permission. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <receiver android:name=".ReminderReceiver" android:exported="false" />
        <!-- Takes the answer given in the shade. Not exported: only our own notification actions reach it. -->
        <receiver android:name=".NoticeActionReceiver" android:exported="false" />

        <!-- Reads the notifications of the banking apps the owner named. Android offers a listener every
             notification on the phone, so the service drops anything from an app that is not on that list
             before it is stored. The owner grants this in the system's own settings screen and can revoke
             it there at any time; until then this service is never bound and captures nothing. -->
        <service
            android:name=".KairosNoticeListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <receiver android:name=".MoneyNoticeReceiver" android:exported="false" />
        <receiver android:name=".QuickAddWidget" android:exported="false" android:label="@string/widget_add">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/quick_add_widget_info" />
        </receiver>

        <!-- WebView renders bundled fonts; native downloadable emoji is unnecessary. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

</manifest>
