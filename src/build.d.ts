/** The short commit this build was made from — see `define` in vite.config.ts. */
declare const __KAIROS_BUILD__: string;
/** The app version from package.json — see `define` in vite.config.ts. */
declare const __KAIROS_VERSION__: string;
