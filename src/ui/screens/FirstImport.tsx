import {Button,Surface} from '../design/primitives';
export function FirstImport({hasAccount,loading,onAccount,onRead}:{hasAccount:boolean;loading:boolean;onAccount:()=>void;onRead:()=>void}){
 return <Surface><h2>Start with your first statement</h2><p>Your financial data stays on this device unless you choose to export it.</p><ol><li>Secure your ledger. Your app lock is already set.</li><li>{hasAccount?'Account ready.':'Add the account shown on your statement.'}</li><li>Choose a statement or transaction export. Kairos reads the dates, balances and transactions where supported.</li><li>Review the extracted rows, then confirm to add them to history.</li></ol><p className="meta">An account here is a local label for your records. It does not connect to your bank. You can record everyday purchases manually at any time.</p><Button variant="primary" disabled={loading} onClick={hasAccount?onRead:onAccount}>{loading?'Reading accounts…':hasAccount?'Read my first statement':'Set up my statement account'}</Button></Surface>;
}
